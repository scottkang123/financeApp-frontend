import { Component } from '@angular/core';

@Component({
  selector: 'app-root', // the name used in other files to denote the app component
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.css']
})
export class AppComponent {
  title = 'finance-app';
}
